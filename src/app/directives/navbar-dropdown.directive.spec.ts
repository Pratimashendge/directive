import { NavbarDropdownDirective } from './navbar-dropdown.directive';

describe('NavbarDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
