import { NavbarLiDirective } from './navbar-li.directive';

describe('NavbarLiDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarLiDirective();
    expect(directive).toBeTruthy();
  });
});
